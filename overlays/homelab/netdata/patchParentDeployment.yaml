---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: netdata-parent
  namespace: netdata
spec:
  template:
    spec:
      containers:
        - name: netdata
          env:
            - name: NETDATA_CLAIM_TOKEN
              value: null
              valueFrom:
                secretKeyRef:
                  name: parent-secrets
                  key: token
            - name: NETDATA_CLAIM_ROOMS
              value: null
              valueFrom:
                secretKeyRef:
                  name: parent-secrets
                  key: rooms
